# Team-Core-
Team Core is a C# console application built with .NET Core Framework and SQL Server. It performs CRUD operations (Add, View, Update, Delete) on employee records through a menu-driven interface.
